#include <stdbool.h>
#include <stdio.h>
int main() {
    bool isPassed=true;
    bool isFailed=false;
    printf("if boolean passed: %d\n",isPassed);
    printf("if boolean failed: %d\n",isFailed);
    return 0;
}